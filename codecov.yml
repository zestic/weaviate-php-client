codecov:
  require_ci_to_pass: true

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0%
        if_not_found: error
    patch:
      default:
        target: auto
        threshold: 0%
        if_not_found: error
